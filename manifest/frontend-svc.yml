apiVersion: v1
kind: Service
metadata:
  name: react-frontend
spec:
  type: NodePort
  selector:
    app: react-frontend
  ports:
    - port: 30335
      protocol: TCP
      targetPort: 3000
      nodePort: 30335